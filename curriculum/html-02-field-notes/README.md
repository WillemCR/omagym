# Field notes article

Publish a small semantic article that works with landmarks, headings and table navigation.

## Your brief

- Create a header (banner) containing navigation named Main with links Notes → #notes and Schedule → #schedule; a main element id notes; and a footer (contentinfo).
- Main contains one article, one h1 named Learning in public, and h2 headings Notes and Schedule in that order. Give the Schedule section id schedule so the navigation link reaches it.
- Inside the article, show a time element with datetime 2026-09-12 and visible date 12 September 2026. Include a figure containing an img with meaningful alt Desk with a notebook (a local inline SVG data URL is fine) and figcaption Weekend setup.
- Provide a table with caption Weekend schedule, column headers Day and Focus (scope col), row headers Saturday and Sunday (scope row), and their data cells Build and Reflect respectively.

## Work and check

Start in `index.html`. The starter intentionally leaves the requirements unfinished. Run the browser suite in Omagym after saving; it opens your app in Chromium and checks the rendered DOM and behavior. Tests reset the page for each case. You may reorganize editable files while preserving the public behavior described above. No external services or downloads are needed. Ask the coach for observations and documentation when you get stuck.

## Documentation

- [MDN: HTML elements](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements)
- [MDN: constraint validation](https://developer.mozilla.org/en-US/docs/Web/HTML/Guides/Constraint_validation)
