import React from 'react';
export default function App() {
  // Build the behavior described in README.md.
  return React.createElement('main', null, React.createElement('h1', null, "Rep counter"));
}
