# Responsive weekend workspace

Assemble a useful responsive dashboard shell with Tailwind utilities.

## Your brief

- Keep .shell containing aside and main, Workspace navigation, and .stats containing three article cards. Style with Tailwind utilities.
- At 1200px, aside is 240px wide to the left of main, both aligned at the top; main fills the remaining shell width. At 390px, aside is above main and both fill the available width.
- Use 24px padding in main. Statistics cards form three equal columns with 16px gaps on desktop and one column with 16px gaps on mobile.
- The shell has at least viewport height, each stats card has 16px padding and 8px radius, and neither layout causes horizontal page overflow.

## Work and check

Start in `index.html`. The starter intentionally leaves the requirements unfinished. Run the browser suite in Omagym after saving; it opens your app in Chromium and checks the rendered DOM and behavior. Tests reset the page for each case. You may reorganize editable files while preserving the public behavior described above. No external services or downloads are needed. Ask the coach for observations and documentation when you get stuck.

## Documentation

- [Tailwind: responsive design](https://tailwindcss.com/docs/responsive-design)
- [Tailwind: hover, focus and other states](https://tailwindcss.com/docs/hover-focus-and-other-states)
